# 📐 Seri47 / BEE Home 제품 스펙 + 해자 + 로드맵 (2026-06-02, 이루)
> 허니비 제품 청사진(2026-06-02)을 이루(Seri47 PM)가 스펙·우선순위로 구조화. 원칙: **경쟁사가 못하는 것 = 우리 가치.** 기능 자체 OR 기능을 유기적으로 엮은 통합. 잘하는 것 + 경쟁사 못하는 것 + 가치순.

---

## 0. 해자 (경쟁사가 못하는 것 — 여기에 집중)
경쟁: **Livin**(중간착취 20%·송금보류·관리인을 *대체*하려 함, 관리인 역량 강화 동기 없음) / **horganice**(임대료 추적 SaaS, 기능 사일로, AI CS·통합 없음).
- **해자 ①: 영속 기관기억(Institutional Memory).** 고객별 CS 히스토리·특성·주의사항이 *시스템*에 누적 → **관리인이 바뀌어도 살아있음.** 임대관리는 보통 관리인 머릿속에서 돌아가다 퇴사 시 증발 = 산업 공통 약점. 이걸 구조 자산으로 만든다. (허니비 헌장 "신뢰는 구조적이지 개인적이지 않다"의 제품화.)
- **해자 ②: 유기적 통합(Orchestration).** LINE + 수금(PromptPay) + 검침(AI) + CS + 방상태 + 청구가 *한 유기체*로 엮여 데이터가 서로 흐름(예: CS 대화가 자동으로 그 방에 태깅, 미납이 CS 노트에 자동 부상). 경쟁사는 *조각*만 한다 — 아무도 합체하지 못한다.
- 둘 다 **소수 AI네이티브 조직만 가능**하고 기존 사일로 SaaS·중개업자가 못 따라옴.

## 1. 시스템 구조 (3층)
```
BEE Home (본사·상위) — 빌딩 다수 통합관리 + 중앙 CS
   └ 빌딩별/고객별 데이터, 각 빌딩에 고객파일 spine 복제
        └ Seri47 (관리인 모드) — 한 빌딩의 운영
             └ [SPINE] 고객파일(=방) : 정보·방상태메모·메시지자동로그·CS히스토리·AI요약
                  ├ 검침(AI 동영상 워크바이)
                  ├ 청구서 발송 (개별 / 일괄, LINE·SMS)
                  ├ 메시지 발송 (개별 / 일괄, LINE·SMS)
                  └ CS (초기 사람 → 데이터 누적 → AI)
```
**핵심: 고객파일(방)이 spine.** 검침·청구·메시지·CS가 전부 거기에 붙어 데이터가 한 곳에 누적된다. (머스크: 20개 기능이 아니라 spine 하나를 만들어라.)

## 2. 기능 명세 (관리인 모드)
- **고객정보 자기입력** — 관리인이 관리자 로그인에서 직접 입력/수정.
- **방상태 메모** — 각 호실 상태 기록.
- **메시지 발송** — 개별 + 일괄, LINE & 핸드폰(SMS).
- **청구서 발송** — 개별 + 일괄, LINE & SMS. (검침→청구 연동.)
- **대화 자동 리스트업** — 주고받은 메시지가 고객파일에 자동 누적.
- **고객 특성·주의사항** — 지난 대화 기반, AI 요약 카드로 관리인에 제시.
- **CS** — 초기 사람, LINE 기존대화 입력 시 자동학습, 고객별 학습·히스토리 누적, 관리자 교체에도 영속.

## 3. BEE Home (상위) 명세
- Seri 같은 다수 고객(빌딩)을 관리. 빌딩별/고객별 데이터.
- 중앙 CS + 각 빌딩 고객파일 spine 복제(공용 컴포넌트 재사용 — 라온 원칙).

## 4. ★LINE 기존대화 퍼올리기 + 활용 솔루션 (허니비 질문)
- **과거(부트스트랩):** LINE 채팅 텍스트 내보내기(앱 기능) → 앱에 업로드 → AI가 고객별 CS 히스토리로 구조화. 수동이나 *1회성*.
- **미래(자동):** 입주자 소통을 *관리인 개인 LINE이 아니라* **BEE LINE OA(@854lohns)** 채널로 이전 → Cloudflare Worker 웹훅이 모든 메시지를 userId↔호실로 매핑해 고객파일에 *자동 로그*. (개인 LINE 메시지는 API로 못 끌어옴 — 그래서 채널을 OA로 옮기는 게 구조 설계의 핵심.)
- **AI 활용:** 고객파일 누적 → AI가 "이 고객 특성·주의사항·지난 이슈" 요약 → CS 보조(초기 사람) → 데이터 쌓이면 AI가 직접. = 해자 ① 실현.
- **구조 결정(중요):** 영속 기억을 얻으려면 소통이 *시스템 채널(OA)*을 통과해야 한다. 개인 LINE에 남으면 자동수집 불가 + 관리인 퇴사 시 증발.

## 5. 로드맵 (경쟁사못함 × 가치 × 순서 — 한번에 다 짓지 않는다)
- **Tier 0 (현 스프린트):** 검침 동영상 워크바이 PoC→완성. *난도 높은 AI 능력 증명.*
- **Tier 1 (다음 — 일일 후크 + spine):** 고객파일(정보·방메모) + 메시지 발송(개별, OA경유) + 자동로그. → **관리인이 *매일* 연다(칫솔테스트)** + 영속기억 시작 + 소통이 시스템으로 유입. *최우선.*
- **Tier 2 (해자 심화):** LINE 과거대화 부트스트랩 + AI 고객요약(주의사항) + 청구 일괄/개별 + 관리자교체 영속.
- **Tier 3 (확장):** BEE Home 본사 — 다빌딩 관리, 중앙 CS, spine 복제.
- **Tier 4 (자율):** 데이터 누적 후 AI가 CS 직접 처리.

## 6. 완성선 규율 (분신 경고)
허니비가 큰 비전을 줬지만 *한번에 다 짓지 않는다*(ADHD·창세 과설계 위험). PM은 *순서*를 소유한다. 일일 후크는 검침(월1회)이 아니라 **메시지/CS(매일)** — 그래서 Tier 1이 진짜 stickiness. 각 Tier는 "관리인이 그걸로 매일 일하는가"로 검증 후 다음.

## 로그
- 2026-06-02 · 허니비 제품청사진 수신→스펙·해자·로드맵 구조화. 해자=영속기억+유기통합. spine=고객파일. LINE솔루션=과거 export부트스트랩+미래 OA자동수집. 다음 실행=Tier0 검침 PoC(관리인 영상 대기), Tier1 spine+메시지 설계 착수.
